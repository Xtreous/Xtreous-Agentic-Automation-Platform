{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "cron-parser": "^5.3.0",
    "encore.dev": "^1.49.1",
    "ioredis": "^5.6.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}